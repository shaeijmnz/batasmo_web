# Default for production builds. Admin > Settings > "Enforce schedule window"
# overrides this at runtime once app_config is seeded in Supabase.
# true = consultation chat can be opened any time after payment.
REACT_APP_BYPASS_CHAT_WINDOW=true
